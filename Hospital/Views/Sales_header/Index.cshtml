@model IEnumerable<Hospital.Models.DataBase.Sales_header>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.customerID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.customer_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.posting_date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.customerID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.customer_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.posting_date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idSales_header }) |
            @Html.ActionLink("Details", "Details", new { id=item.idSales_header }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idSales_header })
        </td>
    </tr>
}

</table>
