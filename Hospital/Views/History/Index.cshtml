@model IEnumerable<Hospital.Models.DataBase.History>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.hist_type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Patient.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.hist_type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Patient.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idFamily_history }) |
            @Html.ActionLink("Details", "Details", new { id=item.idFamily_history }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idFamily_history })
        </td>
    </tr>
}

</table>
