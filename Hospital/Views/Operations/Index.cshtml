@model IEnumerable<Hospital.Models.DataBase.Operation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.procedure_description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.procedure_cost)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.procedure_description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.procedure_cost)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idprocedure }) |
            @Html.ActionLink("Details", "Details", new { id=item.idprocedure }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idprocedure })
        </td>
    </tr>
}

</table>
